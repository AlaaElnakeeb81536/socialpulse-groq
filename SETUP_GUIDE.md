# 📘 دليل الإعداد الكامل - SocialPulse

## 🎯 الهدف
رفع المشروع على GitHub ونشره على GitHub Pages باستخدام Groq AI (مجاني 100%)

---

## ⚡ الخطوات السريعة (5 دقائق)

### 1️⃣ احصلي على Groq API Key

1. **روحي على:** https://console.groq.com
2. **سجلي حساب جديد** (مجاني بالكامل)
3. **اضغطي على "API Keys"** في القائمة الجانبية
4. **اضغطي "Create API Key"**
5. **انسخي الـ Key** (هيبدأ بـ `gsk_...`)

⚠️ **مهم:** احتفظي بالـ Key في مكان آمن!

---

### 2️⃣ ارفعي المشروع على GitHub

#### الطريقة اليدوية (سهلة):

1. **روحي على GitHub.com**
2. **اضغطي "New Repository"** (الزرار الأخضر)
3. **املي البيانات:**
   - Repository name: `socialpulse`
   - Description: `AI-powered social media trend tracker`
   - Public ✅
   - **لا تختاري** "Add README" أو أي حاجة تانية
4. **اضغطي "Create Repository"**

5. **ارفعي كل الملفات اللي حمّلتيها:**
   - اضغطي **"uploading an existing file"**
   - اسحبي **كل المجلدات والملفات**
   - ⚠️ **ما ترفعيش** `.env` (لو موجود)
   - اضغطي **"Commit changes"**

---

### 3️⃣ ضيفي API Key في GitHub Secrets

1. **في الـ Repository بتاعك، روحي:**
   - **Settings** (آخر تاب فوق)
   - **Secrets and variables** → **Actions** (من القائمة اليسار)

2. **اضغطي "New repository secret"**

3. **حطي البيانات:**
   - **Name:** `VITE_GROQ_API_KEY`
   - **Secret:** الصقي الـ Groq API Key اللي نسختيها
   
4. **اضغطي "Add secret"** ✅

---

### 4️⃣ فعّلي GitHub Pages

1. **روحي Settings → Pages**

2. **تحت "Build and deployment":**
   - **Source:** اختاري **"GitHub Actions"** 
   - (مش Deploy from a branch!)

3. **اضغطي Save**

---

### 5️⃣ شوفي المشروع شغال! 🎉

1. **روحي على تاب "Actions"** (فوق في الريبو)

2. **هتلاقي Workflow اسمه "Deploy to GitHub Pages" شغال** 
   - علامة صفراء 🟡 = لسه بيشتغل
   - علامة خضراء ✅ = خلص بنجاح!
   - علامة حمراء ❌ = في مشكلة

3. **استني 2-3 دقائق**

4. **لما يخلص، روحي Settings → Pages**
   - هتلاقي الرابط ظاهر: `https://username.github.io/socialpulse`

5. **افتحي الرابط** 🚀

---

## 🔧 تخصيص الـ Base URL

لو الـ Repository اسمه `socialpulse`، غيّري في `vite.config.ts`:

```typescript
base: '/socialpulse/',  // ⬅️ حطي اسم الريبو بين الـ /
```

**بعدين:**
```bash
git add vite.config.ts
git commit -m "Update base URL"
git push
```

---

## ❌ حل المشاكل الشائعة

### المشكلة: 404 Not Found
**الحل:** تأكدي إن `base` في `vite.config.ts` صح:
```typescript
base: '/اسم-الريبو-بتاعك/',
```

### المشكلة: الموقع بيفتح بس مش بيجيب Trends
**الحل:** 
1. تأكدي إن `VITE_GROQ_API_KEY` موجود في Secrets
2. تأكدي إن الاسم مظبوط بالظبط
3. اعملي Redeploy:
   - Actions → اضغطي على آخر workflow
   - Re-run jobs

### المشكلة: Build Failed في Actions
**الحل:**
1. تأكدي إن كل الملفات اترفعت صح
2. شوفي الخطأ في Actions logs
3. تأكدي إن `package.json` موجود

---

## 📱 التشغيل المحلي (اختياري)

لو عاوزة تشغليه على جهازك:

```bash
# 1. نزّلي Node.js من nodejs.org

# 2. في Terminal:
cd /path/to/socialpulse
npm install

# 3. اعملي ملف .env
echo "VITE_GROQ_API_KEY=gsk_your_key_here" > .env

# 4. شغّلي المشروع
npm run dev

# 5. افتحي: http://localhost:5173
```

---

## 🆘 محتاجة مساعدة؟

لو عندك أي مشكلة:

1. **تأكدي من الخطوات فوق** ✅
2. **شوفي الأخطاء في Console** (F12 في المتصفح)
3. **شوفي Actions logs** في GitHub
4. **اسأليني!** 😊

---

## 🎉 تهانينا!

دلوقتي عندك موقع AI-powered شغال على الإنترنت مجاناً! 🚀

**شاركي الرابط مع أصحابك:** 
`https://username.github.io/socialpulse`

---

Made with ❤️ by SocialPulse Team
